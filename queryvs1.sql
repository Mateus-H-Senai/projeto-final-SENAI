ALTER TABLE comida
ADD nome VARCHAR(20);

CREATE TABLE COMIDA (
tempero VARCHAR(50),
ingredientes VARCHAR(50),
receita VARCHAR(50)
);

CREATE TABLE USUARIO (
nome VARCHAR(50),
email VARCHAR (50),
senha INT(10)
);

